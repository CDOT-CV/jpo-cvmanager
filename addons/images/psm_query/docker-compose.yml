version: '3'
services:
  jpo_psm_query:
    build: .
    image: jpo_psm_query:latest
    restart: always
    environment:
      MONGO_DB_URI: ${MONGO_DB_URI}
      MONGO_DB_NAME: ${MONGO_DB_NAME}
      MONGO_PSM_INPUT_COLLECTION: ${MONGO_PSM_INPUT_COLLECTION}
      MONGO_GEO_OUTPUT_COLLECTION: ${MONGO_GEO_OUTPUT_COLLECTION}
      LOGGING_LEVEL: ${LOGGING_LEVEL}
    logging:
      options:
        max-size: '10m'
        max-file: '5'
