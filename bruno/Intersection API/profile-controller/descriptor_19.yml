info:
  name: descriptor_19
  type: http
  seq: 21
  tags:
    - profile-controller

http:
  method: GET
  url: "{{baseUrl}}/profile/snmpProtocols"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/profile/snmpProtocols"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/profile/snmpProtocols"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/alps+json
      body:
        type: json
        data: ""
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/profile/snmpProtocols"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/schema+json
      body:
        type: json
        data: |-
          {
            "title": "",
            "description": "",
            "properties": {},
            "requiredProperties": [],
            "definitions": {},
            "type": "",
            "$schema": ""
          }
