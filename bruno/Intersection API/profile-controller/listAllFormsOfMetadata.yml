info:
  name: listAllFormsOfMetadata
  type: http
  seq: 1
  tags:
    - profile-controller

http:
  method: GET
  url: "{{baseUrl}}/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/profile"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/hal+json
      body:
        type: json
        data: |-
          {
            "_links": {}
          }
